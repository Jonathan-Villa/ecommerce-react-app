import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(() => ({
  root: {},
  txtField: {
    paddingTop: "10px",
    paddingBottom: "10px",
  },
  txtGroupWrapper: {
    display: "flex",
  },

  addressWrapper: {},

  emailPhoneWrapper: {},
}));

export default useStyles;
